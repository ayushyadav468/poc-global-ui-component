'use client';
import { Pill } from '@monsterindia/global-ui-components';

const PillComponent = () => {
  return <div>
    <Pill id='hello' displayName='Pill' selected={true}/>
  </div>
}

export default PillComponent;